#include <iostream>
#include <vector>

int main() {

    int n{50};

    std::vector<float> volumes(n);
    std::vector<float> frequencies(n);

    for (int i = 0; i < n; ++i)
    {
        
    }

    return 0;
}