#ifndef READ_NUMBER_H
#define READ_NUMBER_H

// This is the content of the .h file, which is where the declarations go
int readNumber(); // function prototype for add.h -- don't forget the semicolon!

#endif

#ifndef WRITE_NUMBER_H
#define WRITE_NUMBER_H

// This is the content of the .h file, which is where the declarations go
void writeAnswer(int x, int y); // function prototype for add.h -- don't forget the semicolon!

#endif